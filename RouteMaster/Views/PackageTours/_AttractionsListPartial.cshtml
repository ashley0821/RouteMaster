@model IEnumerable<RouteMaster.Models.EFModels.Attraction>




    <style>
        thead {
            background-color: lightblue;
            color: black;
        }
    </style>

    <style>
        .addAttraction {
            background-color: lightcoral
        }

        .hidetable {
            display:none;            
        }
    </style>






    <div>
        <h2>景點一覽</h2>
        @*<input type="search" class="form-control form-control-sm" id="attPartialSearch">
        <button type="button" id="attPartialSearchBtn">搜索</button>*@
    </div>


<button id="hideAttractionTableBtn" class="btn btn-primary">顯示/隱藏</button>
    <div class="hidetable" id="attractionTableContainer">
        <table class="table table-striped table-hover" id="attractionTable">
            <thead>
                <tr>
                    <th>
                        景點種類
                    </th>
                    <th>
                        縣市
                    </th>
                    <th>
                        鄉鎮
                    </th>
                    <th>
                        景點名稱
                    </th>
                    <th>
                        詳細地址
                    </th>
                    <th>
                        景點介紹
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody id="targetAttractionTBody">

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.AttractionCategory.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Region.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Town.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Address)
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>


                        <td>
                            <button class="btn btn-sm btn-primary addAttraction" data-id="@item.Id">Add</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>







    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        //$(document).ready(function () {
        //    $('#attPartialSearchBtn').on('click', function () {
        //        var searchKeyword = $('#attPartialSearch').val();
        //        var data = new FormData();
        //        data.append('searchKeyword', searchKeyword);

        //        $.ajax({
        //            url: '/PackageTours/SearchAttraction',
        //            method: 'POST',
        //            headers: {
        //                'X-Requested-With': 'XMLHttpRequest'
        //            },
        //            data: data,
        //            processData: false,
        //            contentType: false,
        //            dataType: 'json',
        //            success: function (data) {
        //                var targetTBody = $('#targetAttractionTBody');
        //                targetTBody.empty();

        //                $.each(data, function (index, item) {
        //                    var tr = $('<tr></tr>');
        //                    var td1 = $('<td></td>').text(item.CategoryName);
        //                    tr.append(td1);

        //                    var td2 = $('<td></td>').text(item.Region);
        //                    tr.append(td2);

        //                    var td3 = $('<td></td>').text(item.Town);
        //                    tr.append(td3);

        //                    var td4 = $('<td></td>').text(item.Name);
        //                    tr.append(td4);

        //                    var td5 = $('<td></td>').text(item.Address);
        //                    tr.append(td5);

        //                    var td6 = $('<td></td>').text(item.Description);
        //                    tr.append(td6);

        //                    targetTBody.append(tr);
        //                });
        //            },
        //            error: function (error) {
        //                console.error('沒傳回前端');
        //            }
        //        });
        //    });
        //});


        
    </script>

<script>
    let attractionTable = document.querySelector('#attractionTableContainer');
    document.querySelector('#hideAttractionTableBtn').addEventListener('click', function () {
        attractionTable.classList.toggle('hidetable');
    })
</script>


